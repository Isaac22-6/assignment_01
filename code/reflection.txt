I learned when to use raise ValueError for calculations within functions to improve input validation. 
raise ValueError stops the program to signal if a function recieved a value with the right data type, but wrong value. It was helpful in my edge case by signaling if the argument for party counts is a negative integer or zero.", so proper divison can be enusre for the split_evenly. 
 



See reflection.md for what makes a good reflection — be specific, use the
terminology from class, and make it actionable.
